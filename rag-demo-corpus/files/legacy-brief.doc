Legacy brief 1. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 2. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 3. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 4. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 5. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 6. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 7. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.

Legacy brief 8. Legacy Word content should still preserve extraction provenance and retrieval visibility for React operators.