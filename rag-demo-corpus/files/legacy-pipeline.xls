Legacy worksheet 1. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 2. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 3. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 4. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 5. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 6. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 7. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.

Legacy worksheet 8. Legacy spreadsheet extraction should still expose workbook semantics and retrieval evidence quality.